events {
}
http {
    include /etc/nginx/mime.types;
    server {
        listen 80;
        root /app/dist; 
        index index.html;

        location /api/ {
            proxy_pass http://backend:8000/;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}